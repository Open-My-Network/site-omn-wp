version: '3.8'

services:
  wordpress:
    image: openmynetwork/wordpress-app:latest
    ports:
      - "8080:80"
    environment:
      WORDPRESS_DB_HOST: openmynetworkrds.cb71kivuosfu.us-east-1.rds.amazonaws.com  # RDS endpoint
      WORDPRESS_DB_USER: admin             # RDS username
      WORDPRESS_DB_PASSWORD: mjeRyvpqewm     # RDS password
      WORDPRESS_DB_NAME: omn_dev_database             # RDS database name
    volumes:
      - ./wp-content:/var/www/html/wp-content
      - ./wp-content/plugins:/var/www/html/wp-content/plugins
      - ./wp-content/themes:/var/www/html/wp-content/themes